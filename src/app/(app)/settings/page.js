
import SettingsClient from "@/components/settings/SettingsClient";

export const metadata={
    title:'Settings',
    description:"Manage FocusBoard preferences.",
};

export default function SettingsPage(){
    return(
      
            <SettingsClient/>
       
    );
}